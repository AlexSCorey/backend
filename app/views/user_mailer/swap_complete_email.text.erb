a shift swap is pending your approval

<%= @swap.requesting_user.name %> would like to swap the following shift with <%= @swap.accepting_user.name %> on the <%= @swap.shift.calendar.name %> calendar:

<%= @swap.shift.start_time.in_time_zone(@swap.shift.calendar.time_zone).to_s(:long_ordinal) %>
- <%= @swap.shift.end_time.in_time_zone(@swap.shift.calendar.time_zone).to_s(:long_ordinal) %>

To approve or deny this request, follow this link: <%= @url %>.